general:
  artifacts:
    - "target/wb3-configserver.jar"
machine:
  java:
    version: openjdk8
deployment:
  dockerhub:
    branch: master
    commands:
      - mvn package
      - $DOCKER_HUB_TRIGGER